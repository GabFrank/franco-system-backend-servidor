# ============================================================================
# CI PROFILE — Used by GitHub Actions (SPRING_PROFILES_ACTIVE=ci)
# Disables external services not available in CI environment
# ============================================================================

# Database (overridable via env vars in workflow)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/bodega}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:franco}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:franco}

# Disable SIFEN (no certificates in CI)
app.sifen.enabled=false
sifen.ambiente=test

# Disable scheduled tasks
spring.main.lazy-initialization=true

# Flyway — V0 creates full schema, baseline skips V1-V99.5
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=99.5
spring.flyway.ignore-missing-migrations=true
spring.flyway.out-of-order=true

# Actuator
management.endpoints.web.exposure.include=health,info

# Disable replication sync (no PostgreSQL logical replication in CI)
replication.sync.enabled=false
replication.refresh.enabled=false
